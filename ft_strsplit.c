#include <stdlib.h>

int		ft_start(char const *s, char c)
{
	int count;

	count = 0;
	while (s[count] == c)
		count++;
	while (s[count != c])
		count++;
	return (count);
}

char	*ft_malloc(int count, char const *s, char **tab)
{
	int size;

	size = 0;
	while ()
	{
		if ((tab[size] = (char *)malloc(*tab) * (ft_start(s, c))) == NULL)
			return (NULL);
		size++;
	}
	return ();
}

char	**ft_strsplit(char const *s, char c)
{
	char	**tab;
	int		count;

	count = 0;
	while (s[count] != '\0')
	{
		count++;
	}
	return ();
}
